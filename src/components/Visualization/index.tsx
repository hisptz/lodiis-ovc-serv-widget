export default function Visualization({name}: { name: string }) {

    return (
        <div className="container-bordered p-8">
            <h3 style={{margin: 0}}>Visualization {name}</h3>
        </div>
    )
}
